[build]
builder = "nixpacks"

[deploy]
startCommand = "java -Dserver.port=$PORT -Dspring.profiles.active=prod -jar target/placify-backend-1.0.0.jar"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
healthcheckPath = "/api/test/health"
healthcheckTimeout = 300
